Object naming in the database cannot include:
```
<new line> <space> $ # - <  ? * { } [ ] ( ) & ` ´ = % + ' " ^ ! / \ . ; ¨ ~ : ₺ € | £
```

Attributes naming in the database cannot include:
```
<new line> $ # - <  ? * { } [ ] ( ) & ` ´ = % + ' " ^ ! / \ . ; ¨ ~ : ₺ € | £
```

> No name can be a MHQL keyword!